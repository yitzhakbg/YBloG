[build]
  command = "zola build" # Command to build your site
  publish = "public"     # Directory to publish (Zola's default output)

[build.environment]
  ZOLA_VERSION = "0.21.0" # Specify your desired Zola version (check latest)

# Optional: For preview deploys, set base URL dynamically
# [context.deploy-preview]
  # command = "zola build --base-url $DEPLOY_PRIME_URL"
